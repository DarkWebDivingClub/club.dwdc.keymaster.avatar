[package]
name = "pkcs11-gpg"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
name = "iz_pkcs11_gpg"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
base64 = "0.22"
